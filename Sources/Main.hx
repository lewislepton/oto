package;

import tanuki.Tanuki;
import kha.Color;

class Main {
	public static function main() {
		Tanuki.setup({app:Oto, title:'OTO', width:1280, height:720, bufferwidth:1920, bufferheight:1080, color:Color.White, smooth:true});
	}
}