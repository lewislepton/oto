package tanuki.weapon;

import kha.Canvas;
import kha.graphics2.Graphics;
using kha.graphics2.GraphicsExtension;
import kha.Color;
import kha.Assets;
import kha.Image;

import tanuki.anim.Sprite;

class Bullet extends Sprite {
	public function new(image:Image, width:Float, height:Float){
		super(image, width, height);
	}

	override public function update(){
		super.update();
	}

	override public function render(canvas:Canvas){
		super.render(canvas);
	}
}